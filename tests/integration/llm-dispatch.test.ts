// tests/integration/llm-dispatch.test.ts
//
// M3: Integration test — binary-level ARCHITECTURAL dispatch.
// T-M3-1.8 (conditional on TEO_TEST_MODE + claude availability)

import { describe, it } from 'vitest';

describe('T-M3-1.8 — ARCHITECTURAL dispatch end-to-end', () => {
  it.todo(
    'T-M3-1.8 — binary-level ARCHITECTURAL dispatch: substantive LLM output, no stub text. ' +
    'REQUIRES: PTY spawner or TEO_TEST_MODE bypass + claude CLI authenticated.',
  );
});
