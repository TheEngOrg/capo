# TEO — Team Orchestration Engine

TEO is a non-invasive, self-contained skill that brings deterministic multi-agent orchestration into existing CLI coding tools (Claude Code, Cursor, etc.). It coordinates specialist agents (engineer, QA, design, security) through a structured plan schema, enforcing gate checks and an audit ledger at each step — without modifying the host tool.

This repo is the v5 fresh build.
