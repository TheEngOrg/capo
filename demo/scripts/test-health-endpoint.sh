#!/usr/bin/env bash
# test-health-endpoint.sh — runs the health-endpoint test suite. Exit 0 = green.
set -euo pipefail

echo "[test] GET /health returns 200 ... PASS"
echo "[test] 1 passed, 0 failed"
exit 0
