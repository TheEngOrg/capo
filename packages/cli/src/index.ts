/**
 * @teo/cli — TEO CLI binary entry points
 *
 * teo       — REPL loop (interactive use)
 * teo serve — daemon loop (ticket pipeline)
 * teo audit — unified audit log merge
 * teo install — manifest compiler (deferred to scripts/teo-install.mjs per Q2 decision)
 *
 * Population happens in Week 4 of the v1 plan, after @teo/core is built.
 */

export const TEO_CLI_VERSION = '1.0.0-pre.0';
