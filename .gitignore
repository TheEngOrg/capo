node_modules/
dist/
.tsbuildinfo
*.tsbuildinfo
.DS_Store
*.log
.env
.env.local
coverage/
.vitest/
.firecrawl
.workflows

# Bun output
# bun.lockb is committed — required for bun install --frozen-lockfile in CI

# Editor
.vscode/
.idea/
*.swp
*.swo

# Plugin install cache — dropped by `claude plugin install`, never authored
.claude/agents/
.claude/skills/

# TEO session-local state — kept out of git
.claude/memory/
.claude/agent-memory/
.claude/agent-memory-local/
.claude/settings.local.json

# Session handoff scratch — lives under .claude/memory/handoffs/, never committed
HANDOFF.md
/HANDOFF-*.md

# Spike sandbox compiled artifacts (derivable, large)
docs/spikes/SPIKE-002/sandbox/dist/
docs/spikes/SPIKE-002/sandbox/node_modules/

# OQ-3 / spike probe binaries (derivable, large)
/oq3-probe
/oq3-case-*

# Diagnostic probes — throwaway scripts + logs, not deliverables
/probe/

# Repo snapshot dumps — derivable, generated on demand
/repomix-output.xml

# Demo home — contains an HMAC signing key; never commit a key to a repo.
# Regenerate locally with: TEO_HOME=demo/.teo-home npx tsx demo/build-plans.ts
demo/.teo-home/
demo/plans/

# v5 rebuild — M3 archived on disk, already pinned in commit 8fd9d3c
.archive-m3/
settings.v4-snapshot.json

# WS-GO-02: esbuild bundle artifact — built in CI, not committed
bin/teo-run.js
bin/teo-run.js.map

# WS-BUILD-PLUGIN: build artifact — built on demand, not committed
# Anchored to root so src/plugin/ (the source tree) is not affected
/plugin/
